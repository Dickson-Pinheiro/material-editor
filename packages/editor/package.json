{
  "name": "@diagramador/editor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser editor for diagramador documents — paints the engine's display list and edits the JSON behind it",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
